<tr>
  <td><span class="title">Reported Creator</span></td>
  <td>
    <% if @ticket.model.present? %>
      <%= link_to(@ticket.model.name, creator_path(id: @ticket.model.id)) %>
    <% else %>
      <span class="text-error">Creator has been deleted</span>
    <% end %>
  </td>
</tr>
